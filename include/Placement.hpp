#include <iostream>

namespace Placement
{
    class Solver
    {
    private:
        /* data */
    public:
        Solver();
        ~Solver();
    };

    Solver::Solver()
    {
        std::cout << "Hello Placement" << std::endl;
    }

    Solver::~Solver()
    {
    }

} // namespace Placement
