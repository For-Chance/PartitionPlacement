#include <iostream>

namespace Partition
{
    class Solver
    {
    private:
        /* data */
    public:
        Solver();
        ~Solver();
    };

    Solver::Solver()
    {
        std::cout << "Hello Partition" << std::endl;
    }

    Solver::~Solver()
    {
    }

} // namespace Partition
